interface:
  display_name: "Team Dev"
  short_description: "Coordinate explicit multi-agent implementation"
  default_prompt: "Use $team-dev to coordinate this substantial repository change with explicit value priorities, disjoint worker ownership, integrated review, verification, and PR publication only when authorized."
policy:
  allow_implicit_invocation: false
