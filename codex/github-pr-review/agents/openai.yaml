interface:
  display_name: "GitHub PR Review"
  short_description: "Review GitHub PRs with actionable, evidence-backed findings."
  default_prompt: "Use $github-pr-review to review the specified GitHub pull request, inspect CI and prior review context where relevant, and run a skeptical pass before reporting findings."
policy:
  allow_implicit_invocation: true
