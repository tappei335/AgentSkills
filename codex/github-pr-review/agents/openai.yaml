interface:
  display_name: "GitHub PR Review"
  short_description: "Review complete PR diffs with evidence-backed findings."
  default_prompt: "Use $github-pr-review to inspect every changed code hunk and report only actionable, evidence-backed findings after a skeptical pass."
policy:
  allow_implicit_invocation: true
