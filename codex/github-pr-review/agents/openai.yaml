interface:
  display_name: "GitHub PR Review"
  short_description: "Review GitHub PRs with actionable, evidence-backed findings."
  default_prompt: "Use $github-pr-review to inspect this change and report only actionable, evidence-backed findings after a skeptical pass."
policy:
  allow_implicit_invocation: true
